allOf:
  - $ref: '#/components/schemas/MenuKey'
  - type: object
    required:
      - relationships
    properties:
      relationships:
        type: object
        required:
          - cafe
        properties:
          cafe:
            type: object
            required:
              - data
            properties:
              data:
                $ref: '#/components/schemas/CafeKey'